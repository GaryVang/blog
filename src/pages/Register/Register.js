import React from 'react';

const Register = ({}) => {
    const x = console.log('User Profile');
    
    return (
        <div>
            Register
        </div>
    )
};

export default Register;