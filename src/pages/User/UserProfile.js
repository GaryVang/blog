import React from 'react';

const UserProfile = ({}) => {
    const x = console.log('User Profile');
    
    return (
        <div>
            User Profile
        </div>
    )
};

export default UserProfile;